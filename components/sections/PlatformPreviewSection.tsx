'use client';
import { useState } from 'react';import { motion } from 'framer-motion';import SectionTitle from '../ui/SectionTitle';
const tabs=['Service Overview','Incident Intelligence','Automation Center','Executive Dashboard'];
export default function PlatformPreviewSection(){const [t,setT]=useState(tabs[0]);return <section className='mx-auto max-w-7xl px-6 py-14'><SectionTitle title='Everything You Need. All in One Platform.'/><div className='grid lg:grid-cols-2 gap-6'><ul className='space-y-2'>{['Real-time Monitoring & Alerting','AI Root Cause Analysis','Incident Correlation & Deduplication','Automation & Self-Healing','Business Service Mapping','Custom Dashboards & Reports','Multi-tenant & Role-based Access'].map(f=><li key={f}>✓ {f}</li>)}</ul><div className='section-card p-5'><div className='flex flex-wrap gap-2'>{tabs.map(x=><button onClick={()=>setT(x)} className={`px-3 py-2 rounded-lg text-sm ${t===x?'bg-limeBrand':'bg-black/5'}`} key={x}>{x}</button>)}</div><motion.div key={t} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className='mt-4 rounded-xl bg-[#111] text-white p-5 min-h-44'>{t} dashboard preview with interactive telemetry widgets.</motion.div></div></div></section>}
