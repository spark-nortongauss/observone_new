import SectionTitle from '../ui/SectionTitle'; import MetricCard from '../ui/MetricCard'; import { proof } from '@/lib/constants';
export default function ProofSection(){return <section className='mx-auto max-w-7xl px-6 py-14'><SectionTitle title='Proven Results. Measurable Impact.' subtitle='ObservOne helps teams achieve more with less.'/><div className='grid md:grid-cols-4 gap-4'>{proof.map(([v,l])=><MetricCard key={l} value={v} label={l}/>)}</div><blockquote className='section-card p-6 mt-6'>“ObservOne changed the way we operate. Our team is no longer reacting to alerts — we’re proactively preventing issues.”<p className='mt-2 text-sm text-gray-600'>Director of Network Operations, Leading Telecom Provider</p></blockquote></section>}
